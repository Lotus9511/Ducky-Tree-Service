import type React from "react"
import type { Metadata } from "next"
import { Inter, Playfair_Display } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _inter = Inter({ subsets: ["latin"] })
const _playfair = Playfair_Display({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Ducky's Tree Service | Professional Tree Removal & Care",
  description:
    "Professional tree removal, trimming, and stump grinding services. Licensed & insured local crew. Fast quotes, same-week service. Call today for a free estimate!",
  keywords: "tree removal, tree trimming, stump grinding, tree service, emergency tree service, storm damage cleanup",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
